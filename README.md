# IPLAnalysisPandas
You are building a data analysis pipeline on IPL datasets using Pandas. The goal is not just analysis, but to simulate a real-world data workflow:  Ingest → Clean → Transform → Analyze → Report → Export  You will work with two datasets:  deliveries.csv (ball-by-ball data) matches.csv (match-level metadata)
